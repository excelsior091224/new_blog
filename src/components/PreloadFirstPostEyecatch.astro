---
const { posts } = Astro.props;
---

{
  posts.map((post: any, index: number) =>
    post.eyecatch && (index === 0 || index === 1) ? (
      <link
        rel="preload"
        as="image"
        href={`${post.eyecatch.url}?fm=webp`}
      />
    ) : null
  )
}
